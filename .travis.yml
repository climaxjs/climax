language: node_js

node_js:
  - "10"

os:
  - linux
  - osx
  # - windows

cache:
  yarn: true
  directories:
    - node_modules

script:
  - yarn test
  - |
    if [ "$TRAVIS_OS_NAME" == "linux" ]; then
      bash scripts/ci/test_coverage.sh
    fi
